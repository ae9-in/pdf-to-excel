# ── Web framework ──────────────────────────────────────────
fastapi>=0.111.0
uvicorn>=0.30.1
python-multipart>=0.0.9

# ── PDF extraction ─────────────────────────────────────────
pdfplumber>=0.11.0
pypdf>=4.2.0

# ── OCR (optional — requires Tesseract binary on the server)
pdf2image>=1.17.0
pytesseract>=0.3.13
pillow>=10.3.0

# ── Excel generation ───────────────────────────────────────
openpyxl>=3.1.5

# ── Data validation ────────────────────────────────────────
pydantic>=2.7.1

# ── MongoDB Connection ──────────────────────────────────────
pymongo>=4.6.0
dnspython>=2.6.0

# ── Env & config ──────────────────────────────────────────
python-dotenv>=1.0.1

# ── Word document extraction (cross-platform) ─────────────
python-docx>=1.2.0
docx2txt>=0.9

# ── pywin32: Windows-only (needed locally for .doc files)
# On Linux/Mac (Vercel/Railway), this package is not available.
# The code uses it only inside a try/except fallback — safe to omit.
# pywin32>=306
